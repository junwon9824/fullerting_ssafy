# MySQL 루트 비밀번호를 저장하는 Secret
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  # MySQL Deployment에서 참조하는 키: MYSQL_ROOT_PASSWORD
  # 값: ssafy 을 base64로 인코딩 (필수)
  MYSQL_ROOT_PASSWORD: c3NhZnk=

  # 🚨 FIX: 백엔드가 참조하는 누락된 키 추가 (값: 'root'의 Base64)
  MYSQL_USER: cm9vdA==
  # 🚨 FIX: 백엔드가 참조하는 누락된 키 추가 (값: 'ssafy'의 Base64)
  MYSQL_PASSWORD: c3NhZnk=
