name: Fullerting CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  backend:
    runs-on: self-hosted # VirtualBox?˜ ?Ÿ¬?„ˆë¥? ?‚¬?š©?•˜?„ë¡? ë³?ê²?
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "maven" # Gradle?„ ?‚¬?š©?•˜?‹ ?‹¤ë©? 'gradle'ë¡? ë³?ê²½í•´ì£¼ì„¸?š”.

      - name: Build and test with Maven
        # Gradle?„ ?‚¬?š©?•˜?‹ ?‹¤ë©? './gradlew build'ë¡? ë³?ê²½í•´ì£¼ì„¸?š”.
        run: mvn -B package --file pom.xml

    # ì£¼ì„ ?•´? œ?•˜?—¬ Docker ë¹Œë“œ ë°? ?‘¸?‹œ ê¸°ëŠ¥ ?™œ?„±?™” ê°??Š¥
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    #
    # - name: Build and push Docker image
    #   uses: docker/build-push-action@v5
    #   with:
    #     context: ./backend
    #     push: true
    #     tags: your-docker-repo/fullerting-backend:latest

  frontend:
    runs-on: self-hosted # VirtualBox?˜ ?Ÿ¬?„ˆë¥? ?‚¬?š©?•˜?„ë¡? ë³?ê²?
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20" # ?”„ë¡œì ?Š¸?˜ Node.js ë²„ì „?— ë§ê²Œ ?ˆ˜? •?•´ì£¼ì„¸?š”.
          cache: "npm" # yarn?„ ?‚¬?š©?•˜?‹ ?‹¤ë©? 'yarn'?œ¼ë¡? ë³?ê²½í•´ì£¼ì„¸?š”.

      - name: Install dependencies
        # yarn?„ ?‚¬?š©?•˜?‹ ?‹¤ë©? 'yarn install'ë¡? ë³?ê²½í•´ì£¼ì„¸?š”.
        run: npm install

      - name: Build
        # yarn?„ ?‚¬?š©?•˜?‹ ?‹¤ë©? 'yarn build'ë¡? ë³?ê²½í•´ì£¼ì„¸?š”.
        run: npm run build
